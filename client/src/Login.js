import React from 'react'

export default function Login() {

    // need to load login page, make a fetch that will redirect to

  return (
    <div>
      <button as="a" href="http://localhost:3000/login">Log in</button>  
    </div>
  )
}
